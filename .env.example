# Django Secret Key (Generate a new secure random string for production)
SECRET_KEY=django-insecure-your-secret-key-here

# Debug Mode (set to False in production)
DEBUG=True

# Allowed Hosts (comma-separated list of hostnames, e.g., yourdomain.com,localhost)
ALLOWED_HOSTS=localhost,127.0.0.1
